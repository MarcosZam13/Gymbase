{
  "name": "gymbase",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:gymbase": "turbo run dev --filter=@memberbase/gymbase",
    "build:gymbase": "turbo run build --filter=@memberbase/gymbase",
    "dev:iron-gym": "pnpm --filter=@memberbase/gymbase dev:iron-gym",
    "dev:zenith-club": "pnpm --filter=@memberbase/gymbase dev:zenith-club"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
